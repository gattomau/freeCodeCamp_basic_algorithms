
function convertToF(celsius) {
    // Applying the algorithm to convert celsius degrees to fahrenheit: divide the celsius value by 9/5, then adding 32
    let fahrenheit = celsius * (9/5) + 32;
    return fahrenheit;
}